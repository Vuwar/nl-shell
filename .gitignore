__pycache__/
*.pyc
.venv/
venv/
.env
ai_shell_gui/frontend/node_modules/
ai_shell_gui/frontend/dist/

# PyInstaller and wheel output. Anchored to the root so they don't also swallow
# a source directory that happens to be called build/ or dist/ somewhere deeper.
/build/
/dist/
*.egg-info/

# Working notes from the design-and-plan workflow: useful while a feature is
# being built, not part of the project. What they produced lives in the code,
# the tests and the README instead.
docs/superpowers/specs/
docs/superpowers/plans/
docs/test-inputs.txt
